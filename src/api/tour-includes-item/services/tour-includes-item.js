'use strict';

/**
 * tour-includes-item service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::tour-includes-item.tour-includes-item');
